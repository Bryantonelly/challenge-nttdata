spring:
  application:
    name: cuentas-movimientos-ms

  datasource:
    url: jdbc:postgresql://localhost:5432/clientesdb
    username: postgres
    password: iceelgato21B#
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  sql:
    init:
      platform: postgres

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

server:
  port: 8082
#  servlet:
#    context-path: /cuentas-movimientos-ms

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka