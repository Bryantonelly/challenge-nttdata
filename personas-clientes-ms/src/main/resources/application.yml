spring:
  application:
    name: personas-clientes-ms

  datasource:
    url: jdbc:postgresql://localhost:5432/clientesdb
    username: postgres
    password: iceelgato21B#
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  sql:
    init:
      platform: postgres

server:
  port: 8081
#  servlet:
#    context-path: /personas-clientes-ms

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka